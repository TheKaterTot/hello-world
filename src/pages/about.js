import React from "react"
import Header from "../components/header"

export default () => (
  <div style={{ color: `gray` }}>
    <Header headerText="About Gatsby"/>
    <Header headerText="Double Header"/>
    <p>Such wow. Very React.</p>
  </div>
)